export const environment = {
  production: false,
  // Set your Clerk publishable key here to enable auth.
  // Leave as-is to run in local/offline mode (always signed in).
  clerkPublishableKey: 'YOUR_CLERK_PUBLISHABLE_KEY',
};
